apiVersion: v1
kind: Service
metadata:
  name: {{ include "alertmanager-teams-adapter.fullname" . }}
  labels:
    {{- include "alertmanager-teams-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "alertmanager-teams-adapter.selectorLabels" . | nindent 4 }}
